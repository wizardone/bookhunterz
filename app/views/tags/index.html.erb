<h3>Tags: <%=@search_tag %></h3>

<ul class="thumbnails">
<% @reviews_by_tags.each do |review| %>
    <li class ="span4">
    <div>
      <p><%= link_to image_tag(review.image.url), book_review_path(review) %></p>
      <p><%= link_to review.book_name, book_review_path(review)%></p>
      <p><%= review.review.truncate(500) %></p>
    </div>
  </li>
<% end %>
</ul>
